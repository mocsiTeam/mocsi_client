mutation Login($password: String!, $nickname: String!) {
    login(
        input:
        {
            password: $password,
            nickname: $nickname
        }
    ),
    {
        accessToken
        refreshToken
    }
}