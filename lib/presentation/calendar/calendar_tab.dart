import 'package:flutter/material.dart';
import 'package:mocsi_client/presentation/calendar/widgets/calendar_widget.dart';

class CalendarTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: CalendarWidget(),
    );
  }
}
